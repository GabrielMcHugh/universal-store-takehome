{
  "name": "postman-tests",
  "private": true,
  "scripts": {
    "test": "newman run \"Universal Store APIs.postman_collection.json\" -e local.postman_environment.json",
    "test:postman": "wait-on http://localhost:3000/catalog http://localhost:4000/inventory && npm test"
  },
  "devDependencies": {
    "newman": "^6.2.1",
    "wait-on": "^8.0.1"
  }
}